# Production / preview env vars for the frontend (Vite).
# Local dev does NOT need this — Vite proxies /api → http://localhost:8000.
#
# Set in Vercel: Project Settings → Environment Variables.
VITE_API_URL=https://syntrx-api.onrender.com
